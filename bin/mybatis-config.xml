<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<settings>
		<setting name="useGeneratedKeys" value="true"/>
		<setting name="useColumnLabel" value="true"/>
		<!-- 开启驼峰命名转换, 例:payPwd属性自动转为数据库表中的字段pay_pwd  -->
		<setting name="mapUnderscoreToCamelCase" value="true"/>
		<setting name="logImpl" value="LOG4J"/>
	</settings>
	<!-- MyBatis 会在包名下面搜索需要的 Java Bean
		每一个在包 中的 Java Bean，在没有注解的情况下，会使用 Bean 的首字母小写的非限定类名来作为它的别名, 如com.mall.mallInterface.entity.User就是user
	 -->
	<typeAliases>
		<package name="com.ycg.ycexpress.beans"/>
	</typeAliases>
</configuration>